import React, { Component } from 'react';
import { connect } from 'react-redux';

class SearchView extends Component {
  constructor(props){
    super(props);
  };

  render() {
    
  }
};

export default connect(null, null)(SearchView);